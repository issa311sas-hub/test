{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath // empty' | { read -r f; case \"$f\" in *.py) echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PostToolUse\",\"additionalContext\":\"[auto-quality] Pythonファイルが変更されました。型ヒント・docstring・エラーハンドリングの品質を確認してください。\"}}' ;; *.md) echo '{}' ;; *) echo '{}' ;; esac; }",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
